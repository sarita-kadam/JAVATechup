package SerializableDeserialization;

//Serialization = converting an object into a byte stream so it can be: saved to a file, sent over a network.

import java.io.Serializable;

public class Student implements Serializable {

    public int RollName;
    public String Name;

}
